*.vsix
.DS_Store
Thumbs.db

# IDE / local config — never commit
.cursor/

# Local secrets — never commit
.env.local
.env*.local
.git-credentials

# Local release scripts
publish_release.py
publish_release.ps1
release.py

